# Gemini CLI

When asked to use Taskforce, follow `AGENTS.md`.

Use the task contract and workflow node configuration at `.taskforce/`. Keep routine implementation delegated to the configured CLI agents and run the supervisor loop to monitor and correct them.
