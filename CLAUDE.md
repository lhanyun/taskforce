# Claude Code

When asked to use Taskforce, follow `AGENTS.md`.

Prefer the repository-local scripts under `skills/taskforce/scripts/`. Treat the current Claude Code session as the Chief — run the supervisor loop, make decisions, and dispatch interventions.

Do not edit routine feature code unless the user explicitly asks or a correction intervention requires it.
